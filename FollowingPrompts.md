# Following Prompts

The following prompts were used to generate the code for the project. The prompts should be used in the order they are listed after starting with the BasePrompt.md file.

- |
  Update the README with:
  - A very detailed steps to run it local in dev mode, to build a stand alone binary and run it
  - Detailed description of the features
  - How to contribute
  - The stack technology
  - GitHub badges
- |
  Add the Contribution file, SECURITY.md and other basic repositorie files
- As many times as needed to add the features: `Proceed`
- |
  The web ui is less than the half of the screen, It should be responsive and fill the screen of the used device
- |
  Problem persist
- |
  Add some description or instructions to the tool Color Converter
- |
  Add better human readable information to the cron tool format
- |
  Use the favico from https://odinnordico.github.io/assets/media/favicon.ico, download it and replace the one in the project
- |
  In the cron tool, instead of the text `Format: Minute Hour Dom Month Dow` Use `Format: {Minute (0-59)} {Hour (0-23)} {Day (1-31)} {Month (1-12)} {DayOfWeek (0 Sunday-6 Saturday)}` or something similar, very descriptive, maybe a table
- |
  Create a make file to perform usual tasks
- |
  Make the built to depends on clean so each build is a clean one
- |
  Update the Dashboard to have links to the tools, and a description of each tool. Also add the a search bar to search for tools
- |
  Use a theme based on neon green kawasaki lime colors and add a switch to toggle the dark and light mode, dark is the default
- |
  Background color is very blue, change it to kawasaki lime compatible, some gay or something
- |
  Make the command to run the app more configurable, add flags to change the port, host, log level, and other settings
- |
  Add test for all, frontend and backend, ensure the coverage is 80% or more
- |
  Update the README with all the latest changes.
- |
  Add lint to the Makefile, readme and contribution files
- |
  Generate a very extensible and very detailed wiki, including all the features and how to use them, how to add new features, how to build, how to run, how to test, how to deploy, how to contribute, how to use the API, etc
- |
  Ensure that the test coverage is 80% or more, if not, add tests to reach the 80% coverage in all packages, and regenerate the coverage report
- |
  Update the makefile exclude the non covered packages from the test in front and backend
- |
  Update the github scripts to use the makefile targets
- |
  Add a new tool to the project, the tool should be a password generator, it should generate a password with a given length and a given set of characters, it should also have a button to copy the password to the clipboard
- |
  Update the wiki, and readme if corresponds with new tool
- |
  Increase the test coverage of the frontend in the missing files of the report generated by `make test-frontend`
- |
  Add an install section in the readme and the wiki, the binary should be installable with `go install`
- |
  The github build action fails with error:
  ```
  Run go build -v ./cmd/privutil/
    go build -v ./cmd/privutil/
    shell: /usr/bin/bash -e {0}
  stat /home/runner/work/PrivUtil/PrivUtil/cmd/privutil: directory not found
  Error: Process completed with exit code 1.
  ```
- |
  Use the make targets in the github files, update makefile if required
